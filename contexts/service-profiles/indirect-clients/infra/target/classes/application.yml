spring:
  application:
    name: indirect-client-management

  datasource:
    url: jdbc:postgresql://localhost:5432/knight_icm
    username: knight
    password: knight_dev
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    schemas: icm

server:
  port: 8082

logging:
  level:
    com.knight: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO
